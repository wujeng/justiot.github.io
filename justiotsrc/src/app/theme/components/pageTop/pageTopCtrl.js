/**
 * @author a.demeshko
 * created on 22.12.2015
 */
(function () {
  'use strict';

  angular.module('JustIOT.theme.components')
    .controller('pageTopCtrl', pagetopCtrl);

  /** @ngInject */
  function pagetopCtrl($scope, FireFactory) {
    /*
    $scope.company=FireFactory.company();
    $scope.user=FireFactory.user();
    $scope.companys=FireFactory.companys();
    $scope.users=FireFactory.users();
    $scope.targetUser=FireFactory.targetUser();
console.log($scope.users);
    $scope.setUser=function(user) {
      console.log(user);
    }
    */
 }

})();
